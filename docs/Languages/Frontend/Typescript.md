---
title: "Typescript"
---

1. Proxy
   Controller gos through the proxy
   configured in Docker:
2. Build the image react is sent to 9002

proxy
auth

ServiceHost = localHost: 8087/8088/8089
Feature store = public domain




If you want to change something in the proxy - always rebuild the frontend container.
If you want t change the UI - then it should auto update.

SearchRoutes --> render --> Page --> Look for what you're targetting

Ternary

be afraid of useEffect - without the array, it will run is anything changes. Really common trip point!

pattern matching - Strings
- Start with default

React- hookform library (e.g. useForm). REgister the Inputs (e.g. type), watch type, if type - GCS, then show x, if PubuSub, show y
> - Contnrol
> - Select

Console log and see it in the Inspect


Tsting:
- describe = test
- Cypress= e2e

Unit Test


zod = deep=

Front-Library = Ant Design
- what buttons to use etc.

plugin for formatting code: 

